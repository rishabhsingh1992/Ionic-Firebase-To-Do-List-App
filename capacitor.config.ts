import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ionic-Firebase-To-Do-List-App',
  webDir: 'www'
};

export default config;
